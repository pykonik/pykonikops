# Description of authorization role

This role creates deployer user which is responsible for deployments.
From local file which is defined as <ssh_pubkey_file> variable a ssh key is copied to 
remote host.  
